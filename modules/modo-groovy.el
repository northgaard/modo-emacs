;;; modo-groovy.el --- groovy major mode -*- lexical-binding: t -*-
;;; Commentary:

;; Configuration for programming groovy

(straight-use-package 'groovy-mode)
(use-package groovy-mode)

(provide 'modo-groovy)
;;; modo-groovy.el ends here
